{% extends 'base.html.twig' %}

{% block title %}Hello CartesPersoController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="width100">
    {% for carte in cartes %}
        <div class="mt-1 wrapperCenter">
            <p class="mx-5">{{ carte.name }}</p>
            <button class="mx-1"><a href="{{ path('modifCarte')}}">Modifier</a></button>
            <button class="mx-1"><a>Supprimer</a></button>
        </div>      
    {% endfor %}

    <div class="mt-3 wrapperCenter">
        <button><a href="{{ path('creationCarte')}}">Crée votre carte !</a></button>
    </div>

    <div id="cartes" style="display: none;">{{ cartes | json_encode() | raw }}</div>
</div>
{% endblock %}
{% block javascripts %}
{% endblock %}
