<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Yu-Gi-Oh! World{% endblock %}</title>
    <link rel="icon" href="path/to/your/favicon.ico">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.css')}}" />
        <link rel="stylesheet" href="{{ asset('assets/css/yugioh.css')}}" /> {# Ajoutez votre propre fichier de styles Yu-Gi-Oh! #}
        <style>
            body {
                font-family: 'Arial', sans-serif;
            }

            header {
                background-color: #343a40;
                padding: 20px 0;
            }

            header h1 {
                margin: 0;
                color: #fff;
            }

            nav {
                background-color: #212529;
            }

            nav a {
                color: #fff;
                text-decoration: none;
            }

            nav a:hover {
                color: #ffc107;
            }

            .container {
                margin-top: 20px;
            }

            footer {
                background-color: #212529;
                color: #fff;
                text-align: center;
                padding: 10px 0;
                position: absolute;
                bottom: 0;
                width: 100%;
            }
        </style>
    {% endblock %}

    {% block javascripts %}
        <script src="{{ asset("assets/js/bootstrap.js")}}"></script>
    {% endblock %}
</head>
<body>
    <header class="text-center">
        <div class="container">
            <h1 class="my-4">Yu-Gi-Oh! World</h1>
        </div>
    </header>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Cards</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Decks</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Tournaments</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">About Us</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        {% block body %}
            <h2 class="mb-4">Featured Cards</h2>
            <div class="row">
                {% for card in featured_cards %}
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="{{ card.image }}" class="card-img-top" alt="{{ card.name }}">
                            <div class="card-body">
                                <h5 class="card-title">{{ card.name }}</h5>
                                <p class="card-text">{{ card.description }}</p>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endblock %}
    </div>

    <footer>
        <p>&copy; {{ "now"|date("Y") }} Yu-Gi-Oh! World. All Rights Reserved.</p>
    </footer>
</body>
</html>
