<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset("assets/images/yu gi api logo.png")}}">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.css')}}" />
            <link rel="stylesheet" href="{{ asset('assets/css/styles.css') }}"/>
        {% endblock %}

        {% block javascripts %}
            <script src="{{ asset("assets/js/bootstrap.js")}}"></script>
        {% endblock %}
    </head>
    <body>
        <header>
            <!-- Logo on the left -->
            <div>
                <img src="/assets/images/yu gi api logo.png" alt="Yu-Gi-Oh! API Logo">
            </div>

            <!-- Navigation Links right next to the logo -->
            <div class="navigation">
                <a href="{{ path('home') }}">Accueil</a>
                <a href="{{ path('cartesAll') }}">Cartes</a>
                <a href="{{ path('cartesPerso') }}">Personalisation</a>
            </div>

            <!-- Profile Image on the far right -->
            <div class="profile">
                <img class="profile-image" src="/assets/images/pp_magie.png" alt="Profile Image">
            </div>
        </header>
        {% block body %}
        {% endblock %}
        <footer>
            <div>
                &copy; 2024 Yu-Gi-Oh! World. Tous droits réservés.
                <br>
                Suivez-nous sur les réseaux sociaux :
                <a href="#">Facebook</a> | 
                <a href="#">Twitter</a> | 
                <a href="#">Instagram</a>
            </div>
        </footer>
    </body>
</html>
